if game.PlaceId ~= 119538320626412 then
    return
end

local OrionLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/duysira5/Hehe/refs/heads/main/Orion.lua.txt"))()

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Window = OrionLib:MakeWindow({
	Name = "Script riêng của An⭐",
	HidePremium = false,
	SaveConfig = true,
	ConfigFolder = "Config"
})

local Tab = Window:MakeTab({
	Name = "Great premium",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})
local KillCount = 0

local KillLabel = Tab:AddLabel("Great Droid Kills: 0")

task.spawn(function()
    local LastDead = false

    while task.wait(0.5) do
        local Boss = workspace:FindFirstChild("World Mobs")
            and workspace["World Mobs"]:FindFirstChild("Event Mobs")
            and workspace["World Mobs"]["Event Mobs"]:FindFirstChild("Great Droid")

        if Boss then
            local Humanoid = Boss:FindFirstChildOfClass("Humanoid")

            if Humanoid then
                if Humanoid.Health <= 0 then
                    if not LastDead then
                        LastDead = true
                        KillCount = KillCount + 1
                        KillLabel:Set("Great Droid Kills: "..KillCount)
                    end
                else
                    LastDead = false
                end
            end
        end
    end
end)
Tab:AddToggle({
				Name = "Auto Play Again",
				Default = true,
				Callback = function(Value)
					getgenv().AutoPlayAgain = Value

					task.spawn(function()
						while getgenv().AutoPlayAgain do
							task.wait(7.8)

							local atom = workspace:FindFirstChild("World Mobs")
							atom = atom and atom:FindFirstChild("Event Mobs")
							atom = atom and atom:FindFirstChild("Great Droid")

							local dead = false

							if atom and atom:FindFirstChild("Humanoid") then
								if atom.Humanoid.Health <= 0 then
									dead = true
								end
							else
								dead = true
							end

							if dead then
								-- Skill 2 charge + release
								pcall(function()
									game.ReplicatedStorage.Remotes.SkillRemote:FireServer(unpack(chargeArgs))
									task.wait(1)
									game.ReplicatedStorage.Remotes.SkillRemote:FireServer(unpack(releaseArgs))
								end)

								task.wait(3)

								-- Start Dungeon mới
								pcall(function()
									game:GetService("ReplicatedStorage")
									.Packages._Index["sleitnick_knit@1.4.7"]
									.knit.Services.DungeonLobbyService.RF.StartDungeon
									:InvokeServer()
								end)
							end
						end
					end)
				end
			})

local SelectedTool = 2
local AutoEquipTools = false

local Dropdown = Tab:AddDropdown({
	Name = "Auto equiptools dls",
	Default = "2",
	Options = {"1","2","3","4","5","6"},
	Callback = function(Value)
		SelectedTool = tonumber(Value)
	end
})

Tab:AddToggle({
	Name = "Auto EquipTools",
	Default = true,
	Callback = function(Value)
		AutoEquipTools = Value

		if Value then
			task.spawn(function()
				while AutoEquipTools do
					local args = {
						SelectedTool
					}

					game:GetService("ReplicatedStorage")
						:WaitForChild("Packages")
						:WaitForChild("_Index")
						:WaitForChild("sleitnick_knit@1.4.7")
						:WaitForChild("knit")
						:WaitForChild("Services")
						:WaitForChild("ToolService")
						:WaitForChild("RE")
						:WaitForChild("UpdatePlayerToolbarSelection")
						:FireServer(unpack(args))

					task.wait(0.1)
				end
			end)
		end
	end
})


local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")

local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local hrp = character:WaitForChild("HumanoidRootPart")

-- Cập nhật lại character khi respawn
player.CharacterAdded:Connect(function(newChar)
    character = newChar
    hrp = newChar:WaitForChild("HumanoidRootPart")
end)

local AutoGodmode = true
local mainConnection

local HEIGHT_ABOVE = 9.22

Tab:AddToggle({
    Name = "Auto Skill + Godmode V2 (Chạy Liên Tục)",
    Default = true,
    Callback = function(Value)
        AutoGodmode = Value
        
        if Value then
            mainConnection = RunService.Heartbeat:Connect(function()
                if not AutoGodmode then return end
                if not hrp or not hrp.Parent then return end
                
                local bossFolder = workspace:FindFirstChild("World Mobs") 
                                  and workspace["World Mobs"]:FindFirstChild("Event Mobs")
                
                if not bossFolder then return end
                
                -- Tìm boss gần nhất (chỉ boss còn sống)
                local closestBoss = nil
                local closestDist = math.huge
                
                for _, mob in ipairs(bossFolder:GetChildren()) do
                    local humanoid = mob:FindFirstChild("Humanoid")
                    local root = mob:FindFirstChild("HumanoidRootPart")
                    if root and humanoid and humanoid.Health > 0 then
                        local dist = (hrp.Position - root.Position).Magnitude
                        if dist < closestDist then
                            closestDist = dist
                            closestBoss = mob
                        end
                    end
                end
                
                if not closestBoss or not closestBoss:FindFirstChild("HumanoidRootPart") then
                    return
                end
                
                local bossHRP = closestBoss.HumanoidRootPart
                local targetPos = bossHRP.Position + Vector3.new(0, HEIGHT_ABOVE, 0)
                
                -- Fix rơi + giật mạnh
                hrp.Velocity = Vector3.new(0, 0, 0)
                hrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                hrp.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
                
                -- Tween mượt
                local tweenInfo = TweenInfo.new(0.22, Enum.EasingStyle.Sine, Enum.EasingDirection.Out)
                local tween = TweenService:Create(hrp, tweenInfo, {
                    CFrame = CFrame.new(targetPos, bossHRP.Position)
                })
                tween:Play()
                
                -- Auto Skill chỉ khi gần
                if closestDist <= 30 then
                    local args = {
                        "Weapons_20_3",
                        {
                            TargetPos = Vector3.new(-409.91522216796875, 1356.2965087890625, 95.83793640136719)
                        },
                        18
                    }
                    pcall(function()
                        game:GetService("ReplicatedStorage")
                            :WaitForChild("Packages")
                            :WaitForChild("_Index")
                            :WaitForChild("sleitnick_knit@1.4.7")
                            :WaitForChild("knit")
                            :WaitForChild("Services")
                            :WaitForChild("SkillManagerV2")
                            :WaitForChild("RE")
                            :WaitForChild("ExecuteSkill"):FireServer(unpack(args))
                    end)
                end
            end)
        else
            if mainConnection then
                mainConnection:Disconnect()
                mainConnection = nil
            end
        end
    end
})
local AutoLoc = true

Tab:AddToggle({
	Name = "Auto Lộc",
	Default = true,
	Callback = function(Value)
		AutoLoc = Value

		if AutoLoc then
			task.spawn(function()
				while AutoLoc do
					local GreatDroid = workspace:FindFirstChild("World Mobs")
						and workspace["World Mobs"]:FindFirstChild("Event Mobs")
						and workspace["World Mobs"]["Event Mobs"]:FindFirstChild("Great Droid")

					if GreatDroid then
						local args = {
							GreatDroid
						}

						game:GetService("ReplicatedStorage")
							:WaitForChild("Packages")
							:WaitForChild("_Index")
							:WaitForChild("sleitnick_knit@1.4.7")
							:WaitForChild("knit")
							:WaitForChild("Services")
							:WaitForChild("SkillManager")
							:WaitForChild("RE")
							:WaitForChild("LockedOnChanged")
							:FireServer(unpack(args))
					end

					task.wait(0.1)
				end
			end)
		end
	end
})
local AutoSkillXanh = true

Tab:AddToggle({
	Name = "Auto Skill Xanh",
	Default = true,
	Callback = function(Value)
		AutoSkillXanh = Value

		if Value then
			task.spawn(function()
				while AutoSkillXanh do
					local args = {
						"Weapons_22_2",
						{
							targetPos = vector.create(1204.772705078125, 2011.405029296875, 744.3994750976562),
							PauseTimeFrame = 0.016666666666666666,
							animationInstance = game:GetService("ReplicatedStorage").Assets.SkillsV2.Weapons["SSJG Legend Weapon"].Animations["Double Wild Cannon"],
							animationSpeed = 1,
							ResumeOnTimePassed = 1,
							HumCFrame = CFrame.new(
								1241.96044921875, 2020.822021484375, 823.090087890625,
								0.9039086103439331, -0.06293655186891556, 0.4230699837207794,
								-7.279183478203777e-09, 0.989115297794342, 0.14714235067367554,
								-0.4277256429195404, -0.13300324976444244, 0.8940698504447937
							)
						},
						12,
						true
					}

					game:GetService("ReplicatedStorage")
						.Packages._Index["sleitnick_knit@1.4.7"]
						.knit.Services.SkillManagerV2.RE.ExecuteSkill
						:FireServer(unpack(args))

					args[4] = false

					game:GetService("ReplicatedStorage")
						.Packages._Index["sleitnick_knit@1.4.7"]
						.knit.Services.SkillManagerV2.RE.ExecuteSkill
						:FireServer(unpack(args))

					task.wait()
				end
			end)
		end
	end
})
local AutoKillscroid = true

Tab:AddToggle({
    Name = "Auto kill scroid",
    Default = true,
    Callback = function(Value)
        AutoKill = Value

        task.spawn(function()
            while AutoKill do
                local customMob = workspace:FindFirstChild("World Mobs")
                    and workspace["World Mobs"]:FindFirstChild("Event Mobs")
                    and workspace["World Mobs"]["Event Mobs"]:FindFirstChild("CustomMob")

                if customMob and customMob.PrimaryPart then
                    local mobPos = customMob.PrimaryPart.Position
                    
                    -- ==================== LOCK MOB ====================
                    local lockArgs = {
                        customMob
                    }
                    ReplicatedStorage
                        :WaitForChild("Packages")
                        :WaitForChild("_Index")
                        :WaitForChild("sleitnick_knit@1.4.7")
                        :WaitForChild("knit")
                        :WaitForChild("Services")
                        :WaitForChild("SkillManager")
                        :WaitForChild("RE")
                        :WaitForChild("LockedOnChanged")
                        :FireServer(unpack(lockArgs))

                    -- ==================== AUTO SKILL ====================
                    local args = {
                        {
                            Camera = CFrame.lookAt(mobPos + Vector3.new(0, 15, 0), mobPos), -- Camera nhìn xuống mob

                            SkillId = "101",
                            Began = true,

                            CFrame = CFrame.lookAt(mobPos + Vector3.new(0, 5, 0), mobPos), -- CFrame skill

                            ["Typе"] = 1,  -- Lưu ý: chữ "е" là Cyrillic

                            Aim = mobPos
                        }
                    }

                    ReplicatedStorage
                        :WaitForChild("Remotes")
                        :WaitForChild("SkillRemote")
                        :FireServer(unpack(args))
                end

                task.wait(0.00001)
            end
        end)
    end
})
local AutoPhePha = true

Tab:AddToggle({
	Name = "Auto biến hình",
	Default = true,
	Callback = function(Value)
		AutoPhePha = Value

		task.spawn(function()
			while AutoPhePha do
				pcall(function()
					local player = game.Players.LocalPlayer
					local charFolder = workspace:FindFirstChild("Characters")

					if charFolder then
						local char = charFolder:FindFirstChild(player.Name)

						if char and not char:FindFirstChild("Mode") then
							local args = {
								{
									Camera = CFrame.new(
										-28.790475845336914,
										298.4656677246094,
										-91.82173156738281,
										0.8630251884460449,
										0.06684061139822006,
										-0.5007192492485046,
										0,
										0.9912075996398926,
										0.1323155164718628,
										0.5051608085632324,
										-0.11419162154197693,
										0.855437159538269
									),
									SkillId = "10",
									Began = true,
									CFrame = CFrame.new(
										-20.313318252563477,
										294.6113586425781,
										-106.30424499511719,
										0.863025426864624,
										0.06684055924415588,
										-0.5007189512252808,
										-8.083265612413015e-09,
										0.9912076592445374,
										0.1323154717683792,
										0.5051605105400085,
										-0.11419161409139633,
										0.8554373979568481
									),
									["Typ\208\181"] = 1,
									Aim = vector.create(
										4.722629547119141,
										287.9955749511719,
										-149.07611083984375
									)
								}
							}

							game:GetService("ReplicatedStorage")
								:WaitForChild("Remotes")
								:WaitForChild("SkillRemote")
								:FireServer(unpack(args))
						end
					end
				end)

				task.wait(1.5)
			end
		end)
	end
})
local AutoDanh = true

Tab:AddToggle({
    Name = "Auto đánh",
    Default = true,
    Callback = function(Value)
        AutoDanh = Value

        task.spawn(function()
            while AutoDanh do
                local args = {
                    {
                        Camera = CFrame.new(
                            1345.49853515625, 600.545654296875, -2916.877197265625,
                            -0.21036981046199799, -0.36530575156211853, 0.9068055748939514,
                            0, 0.9275626540184021, 0.37366774678230286,
                            -0.977621853351593, 0.07860841602087021, -0.19513118267059326
                        ),
                        SkillId = "1",
                        Began = true,
                        CFrame = CFrame.new(
                            1334.1634521484375, 594.4747924804688, -2914.43798828125,
                            -0.3630794882774353, 6.794644491492363e-08, 0.9317581653594971,
                            -1.2599003618163351e-08, 1, -7.783230415725484e-08,
                            -0.9317581653594971, -3.9998539591579174e-08, -0.3630794882774353
                        ),
                        ["Typе"] = 1,
                        Aim = Vector3.new(
                            1287.5755615234375,
                            594.4747924804688,
                            -2896.283935546875
                        )
                    }
                }

                game:GetService("ReplicatedStorage")
                    :WaitForChild("Remotes")
                    :WaitForChild("SkillRemote")
                    :FireServer(unpack(args))

                task.wait(0.1)
            end
        end)
    end
})
local AutoEnergyBallGreta = true

Tab:AddToggle({
    Name = "Auto enery ball Greta",
    Default = true,
    Callback = function(Value)
        AutoEnergyBallGreta = Value

        task.spawn(function()
            while AutoEnergyBallGreta do
                local args = {
                    {
                        Camera = CFrame.new(
                            1134.539794921875, 2026.8126220703125, 878.4974975585938,
                            -0.9346979260444641, -0.11704222112894058, 0.33562028408050537,
                            0, 0.9442304968833923, 0.32928532361984253,
                            -0.35544317960739136, 0.3077823221683502, -0.8825702667236328
                        ),
                        SkillId = "101",
                        Began = true,
                        CFrame = CFrame.new(
                            1125.9349365234375, 2018.352783203125, 903.3755493164062,
                            0.04905059188604355, -0.003960008732974529, -0.9987884163856506,
                            -0.009050008840858936, 0.9999493360519409, -0.004409058019518852,
                            0.9987552762031555, 0.009255310520529747, 0.04901226609945297
                        ),
                        ["Typе"] = 1,
                        Aim = Vector3.new(
                            1175.8743896484375,
                            2018.5732421875,
                            900.9249267578125
                        )
                    }
                }

                game:GetService("ReplicatedStorage")
                    :WaitForChild("Remotes")
                    :WaitForChild("SkillRemote")
                    :FireServer(unpack(args))

                task.wait(0.15)
            end
        end)
    end
})
local AutoNextArea = true

Tab:AddToggle({
    Name = "Auto Next Area",
    Default = true,
    Callback = function(Value)
        AutoNextArea = Value

        task.spawn(function()
            while AutoNextArea do
                local args = {
                    true
                }

                workspace:WaitForChild("Dungeon")
                    :WaitForChild("Stages")
                    :WaitForChild("0")
                    :WaitForChild("NextArea")
                    :WaitForChild("DungeonNextAreaPad")
                    :WaitForChild("RE")
                    :WaitForChild("Interact")
                    :FireServer(unpack(args))

                task.wait(0.1)
            end
        end)
    end
})
local AutoDestroy = true
local Humanoid = game.Players.LocalPlayer.Character:WaitForChild("Humanoid")

Tab:AddToggle({
    Name = "Auto Destroy",
    Default = true,
    Callback = function(v)
        AutoDestroy = v
    end
})

local LastHealth = Humanoid.Health

task.spawn(function()
    while task.wait(0.1) do
        if AutoDestroy and Humanoid.Health < LastHealth then
            local args = {
                {
                    Camera = CFrame.new(1199.78564453125, 2046.0299072265625, 931.2945556640625, 0.8718764781951904, -0.45379844307899475, 0.1841149628162384, 0.000026226043701171875, 0.37599843740463257, 0.9266203045845032, -0.489725798368454, -0.807893693447113, 0.3278360962867737),
                    SkillId = "108",
                    ["Typ\208\181"] = 1,
                    Began = true,
                    CFrame = CFrame.new(1195.4774169921875, 2035.1888427734375, 930.71826171875, 0.9622994065284729, -0.24931593239307404, 0.10872641950845718, 0.000034881977626355365, 0.39985376596450806, 0.9165789484977722, -0.27199238538742065, -0.882019579410553, 0.38478773832321167),
                    Target = workspace["World Map"]["Droid Underground Venue"].Floor.Floor.Part,
                    Aim = vector.create(1192.5758056640625, 2007.6884765625, 920.4428100585938)
                }
            }

            game:GetService("ReplicatedStorage").Remotes.SkillRemote:FireServer(unpack(args))
        end

        LastHealth = Humanoid.Health
    end
end)
local RemoveBeast = true

Tab:AddToggle({
    Name = "Xoá Beast blabla",
    Default = true,
    Callback = function(v)
        RemoveBeast = v
    end
})

task.spawn(function()
    while task.wait(1) do
        if RemoveBeast then
            local Effects = game:GetService("ReplicatedStorage")
                .ClientSource.Modules.SkillSystems.TransformEffects

            local Beast = Effects:FindFirstChild("Beast")
            if Beast then
                Beast:Destroy()
            end
        end
    end
end)
local AutoBeastV2 = true

Tab:AddToggle({
    Name = "Auto Beast V2",
    Default = true,
    Callback = function(v)
        AutoBeastV2 = v
    end
})

task.spawn(function()
    while task.wait(0.08) do
        if not AutoBeastV2 then
            continue
        end

        local Boss = workspace["World Mobs"]["Event Mobs"]:FindFirstChild("Great Droid")
        local Humanoid = Boss and Boss:FindFirstChildOfClass("Humanoid")

        if Humanoid and Humanoid.MaxHealth > 0 then
            local Percent = math.floor((Humanoid.Health / Humanoid.MaxHealth) * 100)

            if Percent >= 100 or Percent == 40 then
                -- Bật Beast ở đây
                print("Bật Beast", Percent .. "%")
                
            elseif Percent <= 30 then
                -- Không bật Beast
                print("Không bật Beast", Percent .. "%")
            end
        end
    end
end)
local AntiAFK = true

Tab:AddToggle({
    Name = "Anti AFK",
    Default = true,
    Callback = function(v)
        AntiAFK = v

        if v then
            loadstring(game:HttpGet("https://raw.githubusercontent.com/KazeOnTop/Rice-Anti-Afk/main/Wind", true))()
        end
    end
})

if AntiAFK then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/KazeOnTop/Rice-Anti-Afk/main/Wind", true))()
end
